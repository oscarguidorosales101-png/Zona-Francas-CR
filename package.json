{
  "name": "zofranca-cr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:frontend\" \"npm:api\"",
    "frontend": "vite",
    "api": "json-server --watch db.json --port 3001",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "json-server": "0.17.4",
    "vite": "^6.4.3"
  }
}
